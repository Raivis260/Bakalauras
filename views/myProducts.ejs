<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500;600;900&family=Ubuntu:wght@300;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/homeStyles.css">
  <link rel="stylesheet" href="/css/pages.css">
  <meta charset="utf-8">
  <title>Bidit</title>

</head>


<body>
  <%- include ("./partials/header") -%>
  <div class="container">
    <h1>Mano produktai</h1>
    <div class="row">
      <% for (let i=0; i<userProducts.length; i++) { %>
      <div class="col-lg-3 col-md-4 m-3 product-grid text-center">
        <div class="image">
          <a href="/products/<%=userProducts[i]._id %>">
            <img src="../<%=userProducts[i].image[0] %>" class="w-100">
            <div class="overlay">
              <div class="detail">
                View details
              </div>
            </div>
          </a>
        </div>
        <h4 ><%=userProducts[i].name %></h4>
        <h5><%=userProducts[i].price %> €</h5>
        <p> <%=userProducts[i].startTime%> </p>
        <p> <%=userProducts[i].city %> </p>
        <a href="/products/<%=userProducts[i]._id %>" class="btn btn-lg btn-block btn-outline-dark">Atidaryti</a>
        <a class="btn btn-warning btn-block mt-2" name="editButton" href="/products/edit/<%=userProducts[i]._id%>">Redaguoti</a>
        <form action="/products/my-items/<%=userProducts[i]._id%>" method="post">
          <button class="btn btn-danger btn-block mt-2" type="submit" name="deleteButton">Ištrinti</button>
        </form>
      </div>
      <%  }; %>
    </div>
  </div>
<%- include ("./partials/footer") -%>
</body>
