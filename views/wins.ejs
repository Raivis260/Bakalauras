<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500;600;900&family=Ubuntu:wght@300;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/homeStyles.css">
  <link rel="stylesheet" href="/css/pages.css">
  <meta charset="utf-8">
  <title>Bidit</title>

</head>


<body>
  <%- include ("./partials/header") -%>
  <div class="container">
    <h1>Aukciono produktai, kuriuose pirmaujate!</h1>
    <div class="row">
      <% if(products.length == 0) { %>
        <h4 class="m-5">Šiuo metu, Jūs nesate laimėtojo pozicijoj nei vienam aukciono objekte.</h4>
      <% } %>
      <% for (let i=0; i<products.length; i++) { %>
      <div class="col-lg-3 col-md-4 m-3 product-grid">
        <div class="image">
          <a href="/products/<%=products[i]._id %>">
            <img src="../<%=products[i].image[0] %>" class="w-100">
          </a>
        </div>
        <h4 class="text-center"><%=products[i].name %></h4>
        <h5><%=products[i].price %> €</h5>
        <p> <%=products[i].city %> </p>
        <a href="/products/<%=products[i]._id %>" class="btn btn-lg btn-block custom-button">Atidaryti</a>
        <% if(products[i].status === false) { %>
          <button class="btn btn-lg btn-block btn-success" type="button" name="payButton">Mokėti</button>
        <% } %>
      </div>
      <%  }; %>
  </div>
  </div>
<%- include ("./partials/footer") -%>
</body>
