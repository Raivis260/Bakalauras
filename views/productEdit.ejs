<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;500;600;900&family=Ubuntu:wght@300;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/homeStyles.css">
  <link rel="stylesheet" href="/css/pages.css">
  <meta charset="utf-8">
  <title>Bidit</title>

</head>


<body>
  <%- include ("./partials/header") -%>

  <div class="container">
    <h1>Redaguoti produktą</h1>
    <form class="mt-3" action="/products/edit/<%=product._id%>" method="POST">
      <div class="form-group row">
        <label for="inputProductName" class="col-sm-2 col-form-label">Pavadinimas</label>
        <div class="col-sm-10">
          <input type="text" class="form-control" id="inputProductName" name="inputProductName" value="<%=product.name%>">
        </div>
      </div>
      <div class="form-group row">
        <label for="inputCity" class="col-sm-2 col-form-label">Miestas</label>
        <div class="col-sm-10">
          <select class="custom-select" name="citySelect">
            <option value="<%=product.city%>" selected><%=product.city%></option>
            <% for(let i=0; i<cities.length; i++) { %>
              <option value='<%=cities[i].city%>'> <%= cities[i].city %> </option>
            <% } %>
          </select>
        </div>
      </div>
      <div class="form-group row">
        <label for="inputCategory" class="col-sm-2 col-form-label">Kategorija</label>
        <div class="col-sm-10">
          <select class="custom-select" name="categorySelect">
            <option value='<%=product.category%>' selected><%=product.category%></option>
            <% for(let i=0; i<categories.length; i++) { %>
              <option value='<%=categories[i].name%>'> <%= categories[i].name %> </option>
            <% } %>
          </select>
        </div>
      </div>
      <div class="form-group row">
        <label for="inputCity" class="col-sm-2 col-form-label">Būklė</label>
        <div class="col-sm-10">
          <select class="custom-select" name="conditionSelect">
            <option value="<%= productCondition.name %>" selected><%=productCondition.name%></option>
            <% for(let i=0; i<conditions.length; i++) { %>
              <option value='<%=conditions[i].name%>'> <%= conditions[i].name %> </option>
            <% } %>
          </select>
        </div>
      </div>
      <div class="form-group row">
        <label for="inputDescription" class="col-sm-2 col-form-label">Aprašymas</label>
        <div class="col-sm-10">
          <textarea name="productDescription" rows="8" cols="80" placeholder="Trumpai pakomentuokite apie savo produktą."><%=product.description%></textarea>
        </div>
      </div>
      <div class="form-group row">
        <label for="inputProductPrice" class="col-sm-2 col-form-label">Kaina</label>
        <div class="col-sm-10">
          <input type="number" class="form-control" name="inputProductPrice" placeholder="Įveskite savo pradinę kainą." value="<%=product.price%>">
        </div>
      </div>
      <div class="form-group row">
        <button class="btn btn-outline-warning btn-block" type="submit">Redaguoti</button>
      </div>
    </form>
  </div>

  <%- include ("./partials/footer") -%>


</body>
